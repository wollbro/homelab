---
services:
  db:
    container_name: {{ postgres_container_name }}
    image: {{ postgres_image }}
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: {{ postgres_password }}
    ports:
      - 5432:5432
    networks:
      - db

networks:
  db:
    name: db
